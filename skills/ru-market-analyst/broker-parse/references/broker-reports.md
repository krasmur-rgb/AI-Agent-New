# Парсинг брокерских отчётов и свод

Цель шага — достать чистый список позиций по каждому счёту и свести в один
портфель. Брокеры дают разные форматы; ниже — что работает.

## Какой формат просить и чем парсить

- **Структурные holdings/marks первичны — обычно это Excel/XLS.** Если у брокера есть
  выбор формата, проси **Excel, не PDF**: таблицы парсятся чище, часто есть рыночная
  оценка позиций + сверочный итог (как у Альфы — «Стоимость всех позиций»).
- **Но PDF не игнорируй.** Из PDF берём то, чего нет в позиционном Excel: совершённые
  сделки, движение денег/кэш, купонные выплаты, удержанный налог, тип/идентичность
  счёта, признаки плеча, кросс-чек $-номинала по купону. Тип файла определяй **по
  содержимому**, не по имени.
- **Тулчейн (порядок; venv — фолбэк):**
  1. Сначала доступные средства: xlsx-скилл (для `.xlsx`), pdf-скилл/OCR (для PDF).
  2. Если их нет — **локальный/временный python-venv** (`.venv-parse/` или temp; НЕ в
     папках с выводом пользователя, НЕ коммить; извлечённые файлы — локальные рабочие
     артефакты) + `pip install pypdf xlrd` (`pypdf` — текст PDF; `xlrd` — старый
     бинарный `.xls`; `pdfplumber` опц. для структуры таблиц). **Никогда глобальный
     `pip`** (PEP 668).
  3. Если установка/сеть заблокированы — попроси **Excel/CSV/текстовый** экспорт.
  - **Ветвление по типу:** `.xls` → `xlrd`; `.xlsx` → xlsx-скилл; CSV → парсить напрямую.
  - **PDF-текст НЕ всегда в контексте** — извлекай явно (pypdf). Если текст пуст/мусор
    (скан) → OCR (pdf-скилл) или попроси не-скан экспорт.
  - Даже структурный Excel **не доверяй, пока сумма позиций не сошлась с итогом
    брокера** и не проверены даты/охват счёта (см. «Контроль целостности»).

## Контроль целостности (делай всегда)

- **Сверка с итогом брокера.** Брокер печатает свой итог по счёту — бесплатная
  контрольная сумма. Сумма распарсенных позиций vs напечатанный итог: расхождение
  >1% → флаг + перечисли, что могло пропасть. Денежный остаток сверяй по оборотам/
  операциям (сделки, комиссии, налоги, переводы, купоны, конвертации, корпдействия);
  не бьётся — флаг.
- **Полнота отчёта.** Проверь, что загружен весь файл (страницы PDF, наличие
  раздела закрывающих остатков). Любой отчёт без раздела позиций или с нулём
  позиций — подозрителен (не только строка Сбера ниже). Период должен
  соответствовать задаче; если не актуален на дату анализа — пометь stale/mixed-dates
  (см. `../../ru-market-shared/references/sources.md`).
- **Базис оценки.** Если возможно, зафиксируй: рыночная оценка clean/dirty, включён
  ли НКД, дата отчёта vs дата котировки; иначе пометь basis unknown (формула —
  `../../ru-market-shared/references/instruments-rf.md`).
- **РЕПО/шорт/маржа — не путай артефакт с долгом.** Сами по себе строки «РЕПО»/
  «РЕПО 2» и «комиссия за маржинальную торговлю» **НЕ означают плечо** (часто это
  брокерский сервис овернайт-РЕПО или закрытая внутридневная маржа). Реальный
  денежный долг / маржинальное плечо определяй ТОЛЬКО по разделу денежных операций:
  «Задолженность клиента», «Непокрытая позиция», знак исходящего остатка по **каждой
  валюте** (отрицательный = долг; общий итог по счёту может быть плюсовым при минусе
  по отдельной валюте) — детали по ТБанку ниже. Реальные шорты по ЦБ подтверждай
  закрывающими отрицательными количествами / заёмными бумагами в разделе движения
  бумаг и классифицируй отдельно (детект плеча —
  `../../risk-strategy/references/analysis-doctrine.md`), не считай как обычные холдинги.
- **Анти-дабл-каунт.** Строй позиции из ОДНОГО авторитетного источника на
  брокер/счёт/дату (скриншот приложения может быть авторитетен по текущему mark,
  PDF — по остаткам на конец периода); сверь ИИС + брокерский = шапка приложения;
  дедуп по (брокер, счёт, ISIN).

## ТБанк — PDF («Отчёт о сделках и операциях»)

- **Извлекай текст PDF явно** (pypdf и т.п.) — НЕ полагайся на «уже в контексте»
  (часто там только размер файла). Если скан/текст не извлёкся → pdf-скилл/OCR или
  попроси не-скан экспорт / Excel.
- **Ищи разделы по тексту заголовка, не по номеру** — нумерация плавает между
  версиями отчёта; номера ниже — лишь ориентир.
- **Раздел 1.1** — совершённые сделки (цены/количества фактических покупок-продаж;
  бери отсюда цены входа для эквити вместо рыночных оценок).
- **Раздел 2** — операции с деньгами: входящий/исходящий остаток по валютам,
  купоны/выплаты по корпдействиям (подтверждает, что кэрри идёт), налоги, выводы.
- **Раздел 3.1** — движение по ЦБ: входящий остаток → зачисление → списание →
  это и есть позиции на конец периода. Бери закрывающие количества.
- **Раздел 4.1** — справочник бумаг: ISIN, тип («ПАИ ФОНДОВ», «акции обыкн»,
  «обл прочих рез», «Стр.обл.КО рез» = структурная). Полезно для классификации.
- **Инвестор / номер счёта** — в шапке. Тип (ИИС/брокерский) **не всегда явен** в
  отчёте — при неоднозначности спроси пользователя (нужно для ЛДВ/налогов).
- **Плечо/долг определяй ТОЛЬКО по разделу 2 «Операции с денежными средствами»**, а
  не по наличию строк РЕПО или маржкомиссии. Долг есть, если по какой-либо валюте
  выполнено хотя бы одно: «Задолженность клиента» > 0, «Непокрытая позиция» > 0,
  исходящий остаток **отрицательный** (знак «−» = брокер кредитует). Все долговые
  поля по всем валютам = 0 и исходящие остатки неотрицательные → **плеча нет**, даже
  если ниже встречаются «РЕПО» и «комиссия за маржинальную торговлю». Смотри по
  валютам: общий итог по счёту может быть плюсовым при минусе по отдельной валюте.
- **«РЕПО овернайт» (ВНБ, контрагент-брокер) — сервис, а не заём клиента.** Брокер
  берёт бумаги/кэш клиента в овернайт-РЕПО и **платит клиенту** доход; в отчёте это
  доходная операция, а не кредит. Не помечай счёт «с плечом» из-за неё. **Но не делай
  вывод «любое РЕПО безопасно»:** если РЕПО не похоже на сервисное овернайт ТБанка
  (нет ВНБ/контрагента-брокера/дохода клиенту, есть открытая вторая часть или
  ненулевые долговые поля) — проверь направление сделки и раздел денежных операций;
  при нехватке данных помечай leverage unknown, не классифицируй по слову «РЕПО».
- **Маржкомиссия ≠ стоящая заёмная позиция.** Строка «MARGIN \ Комиссия за
  маржинальную торговлю» (раздел 3.5 «удержанная комиссия») может возникнуть от
  **внутридневной** маржи, закрытой в тот же день (задолженность на конец периода =
  0). Это издержка за внутридневной овердрафт, а не открытое плечо на дату отчёта.

## Альфа — XLS

- Бинарный OLE2 `.xls`. **Надёжный путь — python `xlrd`** (через venv, см. «чем
  парсить»): читает листы напрямую; xlrd может выдать warning про размер файла — это
  ок, данные читаются. Если есть LibreOffice — можно конвертить в CSV
  (`--headless --convert-to csv:"Text - txt - csv (StarCalc)":44,34,76`), но он не
  всегда установлен. Нативный `.xlsx` — через xlsx-скилл. Проверь кириллицу (нет
  mojibake) и разделители; колонки находи **по заголовку**, не по offset.
- **Пустой экспорт.** Если в листе «Динамика позиций» только шапка и «В текущий
  момент запуск отчёта невозможен. Попробуйте повторно через …» — отчёт **пустой**.
  Попроси переэкспортировать: **полный период**, лист «Динамика позиций» с колонкой
  «стоимость на конец периода».
- Структура: «Динамика позиций за период» → группы **Валюта / Акции / Облигации /
  Прочее** (фонды). По каждой строке: инструмент, ISIN (в следующей строке),
  кол-во и **стоимость в рублях на начало и конец периода**, место учёта (НРД/СПБ).
- Внизу — «Стоимость всех позиций, руб.» (итог по счёту) и предрасчёт НДФЛ.
- Альфа даёт уже рублёвую рыночную оценку позиций — это удобнее, чем считать
  самому; но всё равно сверь ключевые имена по cbonds (рейтинг/риск).

## Сбер / прочие XLS

- Тот же конвертер. Детект пустого/частичного — **обобщённо** (не только по строке
  Сбера): нет раздела позиций / ноль позиций / только шапка / сообщение вида «запуск
  отчёта невозможен, попробуйте позже» = **отчёт пустой**, данных нет; скажи
  пользователю и попроси перегенерировать в кабинете брокера.

## Скриншоты приложения

- Часто закрывают пробелы, которых нет в PDF/XLS: **точные рыночные стоимости
  позиций**, остатки кэша, доступное плечо («Маржиналка до …»), номинал валютных
  бумаг (через рублёвую стоимость восстанавливается $-номинал).
- Шапка приложения = общий итог по брокеру; вычитанием подсчётов суб-счетов
  (ИИС и т.п.) получаешь брокерский счёт.

## Свод

1. Список позиций по каждому счёту с рублёвой стоимостью.
2. Итог по каждому брокеру + grand total по всем брокерам.
3. **Агрегация имён по всем брокерам** — сложи одинаковые ISIN/эмитентов, прежде
   чем оценивать концентрацию (см. доктрину: кросс-брокерная концентрация).
4. Классифицируй каждую позицию по типу для корзин риска (см. analysis-doctrine).
5. **Gross vs net.** Различай валовые активы (gross = рыночная стоимость длинных
   позиций + положительный кэш) и собственный капитал (net equity = gross −
   задолженность) **только при реальной задолженности > 0** (раздел денежных
   операций); задолженность держи отдельным liability и, если кэш signed, не вычитай
   тот же отрицательный остаток второй раз. При задолженности = 0 gross ≈ net — не
   добавляй лишний «чистый» столбец и не помечай плечо.

## Оценка стоимости

- Облигации: считай по формуле из `../../ru-market-shared/references/instruments-rf.md` (чистая цена +
  НКД, непогашенный/индексированный номинал, ×FX). **Рыночная рублёвая оценка из
  приложения/Альфы точнее — бери её, а формулу используй как сверку.**
- Фонды (TMON, TOFZ, LQDT и т.п.): количество × цена пая (из сделок/приложения).
- Эквити: количество × **рыночная цена** (приложение/MOEX). Цена сделки из отчёта —
  только cost basis / помеченная оценка, не текущий mark; не презюмируй (ошибка с
  Т-Техно: оценил в 9 раз выше, пока не сверил с ценой сделки 350,50 ₽).
