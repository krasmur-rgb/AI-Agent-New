{
  "name": "ai-agent-planner-evaluator",
  "version": "0.1.0",
  "description": "Multi-agent orchestrator: planner → agent → evaluator with contract files and Playwright-based evaluation",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "smoke": "tsx scripts/smoke-test.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
